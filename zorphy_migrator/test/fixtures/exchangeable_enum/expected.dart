enum NavigationType {
  ///A link with an href attribute was activated by the user.
  LINK_ACTIVATED,
  ///A form was submitted.
  FORM_SUBMITTED,
}

enum JsAlertResponseAction {
  ///Confirm that the user hit confirm button.
  CONFIRM,
}

enum URLRequestNetworkServiceType {
  DEFAULT,
  VIDEO,
  BACKGROUND,
}
