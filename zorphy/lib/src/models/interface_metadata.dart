import '../common/classes.dart';

/// Re-export Interface
export '../common/classes.dart' show Interface;

/// Metadata about an interface that a class implements
/// Re-exports InterfaceWithComment for backward compatibility
typedef InterfaceMetadata = InterfaceWithComment;
